<span class='name'><%= "#{person.title} #{person.full_name}".strip %></span>
<div class='address-linear'>
  <%= render partial: 'addresses/show', formats: [:html], locals: {address: person.address} %>
</div>