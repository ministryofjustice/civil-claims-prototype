<tr>
  <%= f.hidden_field :_destroy, :class => "destroy" %>
  <td><%= f.date_field :due_date %></td>
  <!-- :field_attribute, :value => format_method(f.object.field_attribute) %> -->
  <td><%= f.input :amount, label: false %></td>
  <td><%= f.input :arrear, label: false %></td>
  <td>
    <div class="btn btn-default btn-small disabled">Edit</div>
    <%= button_to_remove_fields "X", f ,"arrears" %>
  </td>
</tr>