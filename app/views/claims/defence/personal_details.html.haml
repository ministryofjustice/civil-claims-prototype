= render_defence_navigation
%article
  %h2 Step 1 of 5 - Personal details
  %p.lead Add your personal details as they appear on the form
    
  %section
    = render :partial => 'people/show', :locals => {person: @claim.primary_defendant, options: {class: 'defendant'}}

  = simple_form_for @claim.primary_defendant, :url => { :action => "update" } do |f|
    %section
      %h3 Contact details
      .panel-default

        = toggle_inline_help_link( 'If current address is different from above add contact address', address_fields( @claim.primary_defendant.address ) )

        = f.input :phone
        = f.input :mobile
        = f.input :email

        / Solicitor
        %details
          %summary.form-row This defence is being completed by a solicitor on behalf of the tenant
          %p Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer purus dolor, semper ut sapien non, mattis fermentum purus. Sed tincidunt varius tortor sit amet malesuada. Integer vitae orci at magna laoreet vulputate. Aliquam ac tincidunt nisl.

        



    = render :partial => 'claims/defence/save_continue', :locals => {f: f}
