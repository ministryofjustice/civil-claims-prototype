<div class="panel">
  <div class="panel-heading">Supporting documentation</div>

    <p>Attach any documents to support your claim</p>

    <div>
      <%= link_to_add_fields "attachments", "Select", f, :attachments %>
    </div>

    <br/>

    <div>
      <table id="attachments-table" class="table table-condensed">
        <tbody id="attachments-body">
          <%= f.fields_for :attachments do |builder| %>
            <%= render "attachments/attachment_fields", :f => builder %>
          <% end %>
        </tbody>
      </table>
    </div>

    <p>
      <a href="#">About supporting documentation</a>
    </p>

</div>